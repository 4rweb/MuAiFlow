# CLAUDE.md

<!-- ⚠️ TOKEN WARNING: Every line here is loaded on EVERY Claude Code session.
     Keep it under 80 lines. Move detailed guides to skills (loaded on demand). -->

> Copy to your project root as `CLAUDE.md`. Read automatically by Claude Code.

## Project Overview

[1-2 lines: what it does, stack, main components]

## Commands

```bash
npm run dev        # development
npm run build      # build
npm test           # tests
npm run lint       # lint
npm run db:migrate # database migrations
```

## Architecture

[Folder structure and key modules — terse, not prose]

## Key Patterns

- [auth: how it works]
- [data access: ORM, patterns]
- [API conventions: naming, response format]
- [naming: camelCase vars, PascalCase classes, snake_case DB]

## Don'ts

- [never do X]
- [never commit .env]

## Multi-AI Workflow (MuAiFlow)

**Full docs**: `.ai/SETUP.md`

- **⚠️ No AI sets `HUMAN_APPROVED`** — human only
- If `human_approved_by` is empty → **STOP**
- Cross-review required: DB, auth, external APIs, refactors > 5 files
- Plans: `.ai/plans/TEMPLATE.md`
- Model tiers per task: `reasoning | standard | fast` (see `.ai/SETUP.md` → Token Optimization)
